import { RecherchetitlePipe } from './recherchetitle.pipe';

describe('RecherchetitlePipe', () => {
  it('create an instance', () => {
    const pipe = new RecherchetitlePipe();
    expect(pipe).toBeTruthy();
  });
});
